const sequelize = require('../util/db');
const User = require('../models/userModel');
// Import other models as needed

// Define model relationships if required
// e.g., User.hasMany(OtherModel);

module.exports = {
  User,
  // Export other models here
};
